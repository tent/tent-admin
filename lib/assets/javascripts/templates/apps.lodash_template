<h1 class='page-header'>Apps</h1>

<ul class='unstyled apps'>
  <% _.forEach(apps, function(app) { %>
    <%= partials._app.render({ app: app, auth: app.auth, protected_apps: protected_apps }) %>
  <% }) %>
</ul>
